import { PageStub } from "@/components/page-stub";

export default function ForgotPasswordPage() {
  return (
    <PageStub
      title="Forgot password"
      description="Password reset isn't built yet. It's planned for the next phase of development."
    />
  );
}
