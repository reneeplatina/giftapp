import { PageStub } from "@/components/page-stub";

export default function LoginPage() {
  return (
    <PageStub
      title="Log in"
      description="Login isn't built yet. It's planned for the next phase of development."
    />
  );
}
