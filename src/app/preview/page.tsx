import { PageStub } from "@/components/page-stub";

export default function PreviewPage() {
  return (
    <PageStub
      title="Preview"
      description="Previewing your public profile before sharing is coming in a future phase."
    />
  );
}
